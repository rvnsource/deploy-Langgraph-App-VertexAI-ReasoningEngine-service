- Reasoning Engine is a sort of "Langchain-as-a-Service" in Gooogle Cloud (GCP)
-Reasoning Engine in GCP is part of VertexAI offering 'Managed service' for 
   deploying Agentic Applications in GCP, so that anyone can access it. (may be endpoint required, with permissions provided).
-In program, agent keyword refers to app running in local laptop, whereas remote_agent keyword is an instance
   deployed in GCP Reasoning Engine managed service.
-Need to create a bucket in gs (google storage), as defined in 'STAGING_BUCKET' in code code.
